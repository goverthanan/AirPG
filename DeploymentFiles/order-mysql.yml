apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-sqldb
  labels:
    app: order
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order
      tier: backend
  template:
    metadata:
      labels:
        app: order
        tier: backend
    spec:
      containers:
      - env:
        - name: MYSQL_DATABASE
          value: order
        - name: MYSQL_ROOT_PASSWORD
          value: test
        image: mysql:5.7.22
        name: mysql
#      - name: mysql
#        image: mysql:5.7.22
        ports:
        - containerPort: 3306
